<%- include('partials/head') %>
<div class="app-shell app-shell--with-nav">
  <div class="page-content">
    <h2 style="margin-bottom: var(--space-md);">หาช่าง</h2>

    <div class="card">
      <div class="field" style="margin-bottom: 12px;">
        <label class="field__label" for="skill">สกิลที่ต้องการ *</label>
        <select class="select" id="skill">
          <option value="">กำลังโหลดสกิล...</option>
        </select>
        <div class="field__error" id="skill-error"></div>
      </div>

      <div class="field">
        <label class="field__label" for="province">จังหวัด *</label>
        <select class="select" id="province"><option value="">กำลังโหลด...</option></select>
      </div>
      <div class="field">
        <label class="field__label" for="district">อำเภอ/เขต</label>
        <select class="select" id="district" disabled><option value="">เลือกจังหวัดก่อน</option></select>
      </div>
      <div class="field">
        <label class="field__label" for="subdistrict">ตำบล/แขวง</label>
        <select class="select" id="subdistrict" disabled><option value="">เลือกอำเภอก่อน</option></select>
      </div>

      <label class="switch">
        <input type="checkbox" id="auto-expand" checked>
        <span class="switch__track"></span>
        <span class="switch__label">
          <div class="switch__label-main">ขยายค้นหาอัตโนมัติ</div>
          <div class="switch__label-sub">ไม่เจอในตำบล → ลองอำเภอ → จังหวัด</div>
        </span>
      </label>

      <button class="btn btn--primary btn--block" id="search-btn" style="margin-top: var(--space-md);">🔍 ค้นหา</button>
    </div>

    <div id="results-area"></div>
  </div>
  <%- include('partials/bottom-nav', { activeTab: 'search' }) %>
</div>
<%- include('partials/foot', { pageScript: 'search' }) %>
