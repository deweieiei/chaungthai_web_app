<%- include('../partials/head') %>
<div class="app-shell app-shell--with-header">
  <%- include('../partials/header', { title: 'ยืนยันอีเมล', back: true, backHref: '/profile' }) %>
  <div class="page-content page-content--narrow">
    <div id="verify-area">
      <div class="text-center" style="margin-top: var(--space-lg);">
        <div id="email-icon" style="width: 88px; height: 88px; margin: 0 auto var(--space-md); background: var(--primary-soft); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--primary);">
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-10 5L2 7"/></svg>
        </div>
        <h2 id="email-title" style="font-weight: 800;">อีเมลของคุณ</h2>
        <p class="text-muted" id="email-display">-</p>
      </div>
      <div id="content-area" style="margin-top: var(--space-xl);"></div>
    </div>
  </div>
</div>
<%- include('../partials/foot', { pageScript: 'verify-email' }) %>
