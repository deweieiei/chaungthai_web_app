<%- include('../partials/head') %>
<div class="app-shell app-shell--with-header">
  <%- include('../partials/header', { title: 'ยืนยันเบอร์โทร', back: true, backHref: '/profile' }) %>
  <div class="page-content page-content--narrow">
    <div class="text-center" style="margin-top: var(--space-md);">
      <div id="phone-icon" style="width: 88px; height: 88px; margin: 0 auto var(--space-md); background: var(--primary-soft); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--primary);">
        <svg width="44" height="44" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2"/><path d="M12 18h.01"/></svg>
      </div>
      <h2 id="phone-title" style="font-weight: 800;">เบอร์โทรของคุณ</h2>
      <p class="text-muted" id="phone-display">-</p>
    </div>
    <div id="content-area" style="margin-top: var(--space-lg);"></div>
  </div>
</div>
<%- include('../partials/foot', { pageScript: 'verify-phone' }) %>
